# Phase 2C Runtime Evidence Ledger

This document details the exact execution commands, remote automation scripts, and server-side logs proving native runtime verification in the Android Emulator.

---

## 1. Emulator Debugging & Port Forwarding Configuration

All WebView runtime automation operations were routed via the Chromium Remote Debugging protocol:

1. **AVD Boot Verify**:
   `adb shell getprop sys.boot_completed` returns `1`.
2. **Chromium DevTools Socket Scan**:
   `adb shell cat /proc/net/unix | grep devtools`
   Returned: `@webview_devtools_remote_5807`
3. **Port Forwarding Establishment**:
   `adb forward tcp:9223 localabstract:webview_devtools_remote_5807`
4. **DevTools Targets Query**:
   `http://localhost:9223/json` yields target WebSocket debugger URLs (e.g. `ws://localhost:9223/devtools/page/...`).

---

## 2. WebView Remote Automation Script

The automated test script is located at [webview_test.js](file:///Users/pradhyumupadhyay/.gemini/antigravity-ide/brain/13d93f8d-cc7f-4d4b-a4d6-4fa172c0dc36/scratch/webview_test.js). It programmatically:
1. Connects to port `9223` remote WebSocket target.
2. Evaluates navigation to `https://localhost/signup`.
3. Dispatches React-compatible prototype setter change events to inputs to simulate user typing:
   ```javascript
   function setReactInputValue(input, value) {
     const setter = Object.getOwnPropertyDescriptor(window.HTMLInputElement.prototype, 'value').set;
     setter.call(input, value);
     input.dispatchEvent(new Event('input', { bubbles: true }));
   }
   ```
4. Clicks the submit button, triggering E2EE user keypair generation and REST registration payload.
5. Captures redirect target location, localStorage keys, and login indicators.

---

## 3. Server-Side Log Receipts

The following log traces were generated by the local Express backend on the macOS host (listening on port 5001) during automated WebView execution:

```
2026-07-08 18:55:04 [info]: POST /api/auth/signup
2026-07-08 18:55:05 [info]: [SECURITY AUDIT] User registered. Email: emu_36984@example.com | IP: ::ffff:127.0.0.1
2026-07-08 18:55:06 [info]: GET /api/sync/user?afterSequence=0&limit=50
2026-07-08 18:55:08 [debug]: Secure socket connected: TsE43S7LJ5o17aoOAAAB (emu_36984@example.com)
```

**Conclusion**: The automated WebView script proves that client-server HTTP API calls, cryptographic key generations, and WebSocket authentication handshakes run successfully inside the native Android WebView container.
